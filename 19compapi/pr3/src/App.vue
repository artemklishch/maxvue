<template>
  <section class="container">
    <h2>{{ user.name }}</h2>
    <h3>{{ user.age }}</h3>
    <!-- <button @click="user.age = 34">Change the Age</button> -->
    <button @click="setNewAge">Change the Age</button>
  </section>
</template>

<script>
import { ref, reactive } from 'vue';
export default {
  setup() {
    const uName = ref('Maximilian');
    const uAge = ref(30);
    const user = reactive({
      name: 'Maximilian',
      age: 32
    });
    function setNewAge() {
      user.age = 35;
      uAge.value = 40;
    }
    return { user: user, setNewAge: setNewAge, userName: uName, age: uAge };
  }
  // data() {
  //   return {
  //     userName: 'Maximilian',
  //      age: 31
  //   };
  // },
  // methods:{
  //   setNewAge(){
  //     this.age = 32
  //   }
  // }
};
</script>

<style>
* {
  box-sizing: border-box;
}

html {
  font-family: sans-serif;
}

body {
  margin: 0;
}

.container {
  margin: 3rem auto;
  max-width: 30rem;
  border-radius: 12px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.26);
  padding: 1rem;
  text-align: center;
}
</style>